import { StatusBar } from 'expo-status-bar';
import * as ImagePicker from 'expo-image-picker';
import { useEffect, useMemo, useState, type ReactNode } from 'react';
import { ActivityIndicator, Alert, Image, KeyboardAvoidingView, Platform, Pressable, SafeAreaView, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { authApi, type AuthSession } from './src/auth';
import { clearSession, loadSession, saveSession } from './src/auth-storage';
import { loadFavoriteProductIds, saveFavoriteProductIds } from './src/favorite-storage';
import { chatApi, imageUrl, productsApi, type ChatThread, type Product, type ProductInput, uploadProductImages } from './src/market-api';

type Tab = 'home' | 'products' | 'chat' | 'settings';
type ChatTarget = { product: Pick<Product, 'id' | 'title' | 'askingPrice' | 'imagePaths'>; otherUser: { id: string; nickname: string } };
type Photo = { uri: string; storedPath?: string };
const green = '#0E766E';

export default function App() {
  const [mode, setMode] = useState<'sign-in' | 'sign-up'>('sign-in');
  const [loginId, setLoginId] = useState(''); const [password, setPassword] = useState(''); const [confirmation, setConfirmation] = useState('');
  const [session, setSession] = useState<AuthSession | null>(null); const [restoring, setRestoring] = useState(true); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState('');
  useEffect(() => { loadSession().then(setSession).catch(() => undefined).finally(() => setRestoring(false)); }, []);
  async function submit() {
    const id = loginId.trim().toLowerCase();
    if (!/^[a-z0-9][a-z0-9_-]{3,19}$/.test(id)) return setError('아이디는 영문 소문자, 숫자, 밑줄(_), 하이픈(-)으로 된 4~20자여야 합니다.');
    if (password.length < 8) return setError('비밀번호는 8자 이상으로 입력해 주세요.');
    if (mode === 'sign-up' && password !== confirmation) return setError('비밀번호 확인이 일치하지 않습니다.');
    setSubmitting(true); setError('');
    try { const result = mode === 'sign-up' ? await authApi.signUp(id, password) : await authApi.signIn(id, password); await saveSession(result); setSession(result); }
    catch (caught) { setError(caught instanceof Error ? caught.message : '요청을 처리하지 못했습니다.'); } finally { setSubmitting(false); }
  }
  if (restoring) return <Loading label="계정을 확인하고 있어요" />;
  if (session) return <Marketplace session={session} onSignOut={async () => { await clearSession(); setSession(null); }} />;
  const signingUp = mode === 'sign-up';
  return <SafeAreaView style={s.safe}><StatusBar style="dark" /><KeyboardAvoidingView style={s.flex} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView contentContainerStyle={s.login}><Brand /><Text style={s.loginTitle}>{signingUp ? '회원가입' : '로그인'}</Text><Text style={s.loginSub}>{signingUp ? 'RAMarket에서 RAM 거래를 시작하세요.' : '계정으로 안전하게 거래를 이어가세요.'}</Text><Label text="아이디" /><TextInput value={loginId} onChangeText={setLoginId} autoCapitalize="none" autoCorrect={false} style={s.input} placeholder="아이디를 입력하세요" placeholderTextColor="#89948F" /><Label text="비밀번호" /><TextInput value={password} onChangeText={setPassword} secureTextEntry style={s.input} placeholder="비밀번호를 입력하세요" placeholderTextColor="#89948F" />{signingUp && <><Label text="비밀번호 확인" /><TextInput value={confirmation} onChangeText={setConfirmation} secureTextEntry style={s.input} placeholder="비밀번호를 한 번 더 입력하세요" placeholderTextColor="#89948F" /></>}{error ? <Text style={s.error}>{error}</Text> : null}<Pressable disabled={submitting} onPress={submit} style={[s.primary, submitting && s.disabled]}>{submitting ? <ActivityIndicator color="#fff" /> : <Text style={s.primaryText}>{signingUp ? '회원가입' : '로그인'}</Text>}</Pressable><Pressable onPress={() => { setMode(signingUp ? 'sign-in' : 'sign-up'); setError(''); setPassword(''); setConfirmation(''); }} style={s.secondary}><Text style={s.secondaryText}>{signingUp ? '로그인으로 돌아가기' : '회원가입'}</Text></Pressable></ScrollView></KeyboardAvoidingView></SafeAreaView>;
}

function Marketplace({ session, onSignOut }: { session: AuthSession; onSignOut: () => Promise<void> }) {
  const [tab, setTab] = useState<Tab>('products'); const [products, setProducts] = useState<Product[]>([]); const [selected, setSelected] = useState<Product | null>(null); const [editing, setEditing] = useState<Product | 'new' | null>(null); const [favoriteIds, setFavoriteIds] = useState<string[]>([]); const [favoritesOpen, setFavoritesOpen] = useState(false); const [chat, setChat] = useState<ChatTarget | null>(null); const [loading, setLoading] = useState(true); const [loadError, setLoadError] = useState('');
  async function reload() { setLoading(true); setLoadError(''); try { setProducts(await productsApi.list(session)); } catch (caught) { setLoadError(caught instanceof Error ? caught.message : '상품을 불러오지 못했습니다.'); } finally { setLoading(false); } }
  useEffect(() => { reload(); loadFavoriteProductIds(session.user.id).then(setFavoriteIds).catch(() => undefined); }, [session.user.id]);
  function toggleFavorite(id: string) { setFavoriteIds((current) => { const next = current.includes(id) ? current.filter((value) => value !== id) : [...current, id]; saveFavoriteProductIds(session.user.id, next).catch(() => undefined); return next; }); }
  async function save(input: ProductInput, id?: string) { const result = id ? await productsApi.update(id, input, session) : await productsApi.create(input, session); setProducts((current) => id ? current.map((p) => p.id === id ? result : p) : [result, ...current]); setEditing(null); setSelected(result); }
  function switchTab(next: Tab) { setTab(next); setSelected(null); setEditing(null); setFavoritesOpen(false); setChat(null); }
  const favorites = products.filter((product) => favoriteIds.includes(product.id));
  let content: ReactNode;
  if (editing) content = <ProductForm session={session} product={editing === 'new' ? undefined : editing} onBack={() => setEditing(null)} onSave={save} />;
  else if (selected) content = <ProductDetail product={selected} mine={selected.seller.id === session.user.id} favorite={favoriteIds.includes(selected.id)} onBack={() => setSelected(null)} onFavorite={() => toggleFavorite(selected.id)} onEdit={() => setEditing(selected)} onChat={() => { setSelected(null); setTab('chat'); setChat({ product: selected, otherUser: selected.seller }); }} />;
  else if (favoritesOpen) content = <Favorites products={favorites} onBack={() => setFavoritesOpen(false)} onSelect={setSelected} />;
  else if (chat) content = <ChatRoom session={session} target={chat} onBack={() => setChat(null)} />;
  else if (tab === 'products') content = <ProductList products={products} favoriteIds={favoriteIds} loading={loading} error={loadError} onReload={reload} onSelect={setSelected} onFavorite={toggleFavorite} onWrite={() => setEditing('new')} onFavorites={() => setFavoritesOpen(true)} />;
  else if (tab === 'chat') content = <ChatList session={session} onOpen={(thread) => setChat({ product: thread.product, otherUser: thread.otherUser })} />;
  else if (tab === 'settings') content = <Settings loginId={session.user.loginId} favorites={favorites} onSelect={setSelected} onSignOut={onSignOut} />;
  else content = <Empty title="주간 RAM 시세" body="메인 화면은 준비 중이에요." />;
  return <SafeAreaView style={s.safe}><StatusBar style="dark" /><View style={s.flex}>{content}</View>{!selected && !editing && !favoritesOpen && !chat && <Nav active={tab} onSelect={switchTab} />}</SafeAreaView>;
}

function ProductList({ products, favoriteIds, loading, error, onReload, onSelect, onFavorite, onWrite, onFavorites }: { products: Product[]; favoriteIds: string[]; loading: boolean; error: string; onReload: () => void; onSelect: (p: Product) => void; onFavorite: (id: string) => void; onWrite: () => void; onFavorites: () => void }) {
  const [search, setSearch] = useState(''); const [category, setCategory] = useState('전체'); const visible = useMemo(() => products.filter((p) => (category === '전체' || p.category.includes(category)) && p.title.toLowerCase().includes(search.trim().toLowerCase())), [products, category, search]);
  return <View style={s.flex}>
    <View style={s.header}><Brand compact /><Pressable onPress={onFavorites} style={s.favoriteShortcut}><Text style={s.heartOn}>♥</Text><Text style={s.favoriteCount}>{favoriteIds.length}</Text></Pressable></View>
    <Text style={s.pageTitle}>메모리 상품</Text>
    <View style={s.searchBox}><Text style={s.searchIcon}>⌕</Text><TextInput value={search} onChangeText={setSearch} placeholder="메모리 상품을 검색하세요" placeholderTextColor="#89948F" style={s.searchInput} /></View>
    <ScrollView horizontal showsHorizontalScrollIndicator={false} style={{ flexGrow: 0, flexShrink: 0, maxHeight: 62 }} contentContainerStyle={{ alignItems: 'center', gap: 8, paddingHorizontal: 20, paddingVertical: 11 }}>
      {['전체', 'DDR5', 'DDR4'].map((value) => <Pressable key={value} onPress={() => setCategory(value)} style={[s.category, { alignSelf: 'center', flexGrow: 0, flexShrink: 0, minHeight: 38 }, category === value && s.categoryOn]}><Text style={[s.categoryText, category === value && s.categoryTextOn]}>{value}</Text></Pressable>)}
    </ScrollView>
    <ScrollView contentContainerStyle={s.list}>{loading ? <Loading label="상품을 불러오는 중이에요" /> : error ? <Empty title="상품을 불러오지 못했어요" body={error} action="다시 시도" onAction={onReload} /> : visible.length ? visible.map((p) => <ProductRow key={p.id} product={p} favorite={favoriteIds.includes(p.id)} onSelect={() => onSelect(p)} onFavorite={() => onFavorite(p.id)} />) : <Empty title="등록된 상품이 없어요" body="첫 번째 RAM 상품을 판매해 보세요." />}</ScrollView>
    <Pressable onPress={onWrite} style={s.sell}><Text style={s.sellText}>＋  판매하기</Text></Pressable>
  </View>;
}

function ProductRow({ product, favorite, onSelect, onFavorite }: { product: Product; favorite: boolean; onSelect: () => void; onFavorite: () => void }) { return <Pressable onPress={onSelect} style={s.productRow}><Thumbnail product={product} /><View style={s.productInfo}><Text style={s.memory}>{product.category}</Text><Text numberOfLines={2} style={s.productTitle}>{product.title}</Text><Text style={s.price}>{price(product.askingPrice)}</Text><Text style={s.meta}>{product.seller.nickname} · {relative(product.createdAt)}</Text></View><Pressable onPress={onFavorite} hitSlop={8} style={s.heartButton}><Text style={[s.heart, favorite && s.heartOn]}>{favorite ? '♥' : '♡'}</Text></Pressable></Pressable>; }
function ProductDetail({ product, mine, favorite, onBack, onFavorite, onEdit, onChat }: { product: Product; mine: boolean; favorite: boolean; onBack: () => void; onFavorite: () => void; onEdit: () => void; onChat: () => void }) { return <View style={s.flex}><ScrollView><TopBar title="상품 상세" onBack={onBack} /><Thumbnail product={product} large /><View style={s.detail}><Text style={s.detailTitle}>{product.title}</Text><Text style={s.meta}>{product.seller.nickname} · {relative(product.createdAt)}</Text><Text style={s.detailPrice}>{price(product.askingPrice)}</Text><View style={s.divider} /><View style={s.seller}><View style={s.avatar}><Text style={s.avatarText}>{product.seller.nickname.charAt(0).toUpperCase()}</Text></View><Text style={s.sellerName}>{product.seller.nickname}</Text></View><Text style={s.description}>{product.description}</Text></View></ScrollView><View style={s.actions}><Pressable onPress={onFavorite} style={s.actionHeart}><Text style={[s.heart, favorite && s.heartOn]}>{favorite ? '♥' : '♡'}</Text></Pressable><View style={s.actionInfo}><Text style={s.actionPrice}>{price(product.askingPrice)}</Text><Text style={s.actionMeta}>거래 장소 협의</Text></View><Pressable onPress={mine ? onEdit : onChat} style={s.actionButton}><Text style={s.actionButtonText}>{mine ? '수정하기' : '채팅하기'}</Text></Pressable></View></View>; }

function ProductForm({ session, product, onBack, onSave }: { session: AuthSession; product?: Product; onBack: () => void; onSave: (input: ProductInput, id?: string) => Promise<void> }) {
  const parts = product?.category.split(' · ') ?? ['DDR4', '8GB', '']; const [title, setTitle] = useState(product?.title ?? ''); const [description, setDescription] = useState(product?.description ?? ''); const [amount, setAmount] = useState(product ? String(product.askingPrice) : ''); const [ddr, setDdr] = useState(parts[0] === 'DDR5' ? 'DDR5' : 'DDR4'); const [capacity, setCapacity] = useState(parts[1] ?? '8GB'); const [maker, setMaker] = useState(parts.slice(2).join(' · ')); const [photos, setPhotos] = useState<Photo[]>((product?.imagePaths ?? []).map((path) => ({ uri: imageUrl(path) ?? '', storedPath: path }))); const [error, setError] = useState(''); const [saving, setSaving] = useState(false);
  async function pick() { const permission = await ImagePicker.requestMediaLibraryPermissionsAsync(); if (!permission.granted) return Alert.alert('사진 권한이 필요해요', '판매할 상품 사진을 선택하려면 앨범 접근을 허용해 주세요.'); const result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images'], allowsMultipleSelection: true, selectionLimit: Math.max(1, 8 - photos.length), quality: 0.8 }); if (!result.canceled) setPhotos((current) => [...current, ...result.assets.map((asset) => ({ uri: asset.uri }))].slice(0, 8)); }
  async function save() { const askingPrice = Number(amount.replace(/[^0-9]/g, '')); if (title.trim().length < 2) return setError('상품 제목을 2자 이상 입력해 주세요.'); if (!description.trim()) return setError('상품 설명을 입력해 주세요.'); if (description.trim().length > 500) return setError('상품 설명은 500자까지 입력할 수 있어요.'); if (!askingPrice) return setError('가격을 숫자로 입력해 주세요.'); if (!maker.trim()) return setError('제조사를 입력해 주세요.'); setSaving(true); setError(''); try { const oldPaths = photos.flatMap((photo) => photo.storedPath ? [photo.storedPath] : []); const newPaths = await uploadProductImages(photos.filter((photo) => !photo.storedPath).map((photo) => photo.uri), session); await onSave({ title: title.trim(), description: description.trim(), category: `${ddr} · ${capacity} · ${maker.trim()}`, condition: 'good', askingPrice, imagePaths: [...oldPaths, ...newPaths] }, product?.id); } catch (caught) { setError(caught instanceof Error ? caught.message : '판매글을 저장하지 못했습니다.'); } finally { setSaving(false); } }
  return <KeyboardAvoidingView style={s.flex} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><TopBar title={product ? '판매 글 수정' : '판매 글 작성'} onBack={onBack} /><ScrollView contentContainerStyle={s.form}><Pressable onPress={pick} style={s.imagePicker}><Text style={s.imageIcon}>▧</Text><Text style={s.imageText}>사진 추가</Text><Text style={s.hint}>{photos.length}/8장 · 앨범에서 선택</Text></Pressable>{photos.length ? <ScrollView horizontal contentContainerStyle={s.photos}>{photos.map((photo, index) => <View key={`${photo.uri}-${index}`} style={s.photoWrap}><Image source={{ uri: photo.uri }} style={s.photo} /><Pressable onPress={() => setPhotos((current) => current.filter((_, currentIndex) => currentIndex !== index))} style={s.remove}><Text style={s.removeText}>×</Text></Pressable></View>)}</ScrollView> : null}<Label text="상품 제목" /><TextInput value={title} onChangeText={setTitle} style={s.formInput} maxLength={100} placeholder="예: FURY Beast 16GB 2개" placeholderTextColor="#89948F" /><Label text="카테고리" /><Text style={s.hint}>규격과 용량을 선택하고 제조사를 입력해 주세요.</Text><Choice label="규격" values={['DDR4', 'DDR5']} selected={ddr} onSelect={setDdr} /><Choice label="용량" values={['4GB', '8GB', '16GB', '32GB', '64GB']} selected={capacity} onSelect={setCapacity} /><Text style={s.choiceLabel}>제조사</Text><TextInput value={maker} onChangeText={setMaker} style={s.formInput} maxLength={30} placeholder="예: 삼성전자, SK하이닉스" placeholderTextColor="#89948F" /><Label text="상품 설명" /><TextInput value={description} onChangeText={setDescription} style={[s.formInput, s.descriptionInput]} multiline textAlignVertical="top" maxLength={500} placeholder="상품 상태, 구성품, 거래 방법을 알려주세요." placeholderTextColor="#89948F" /><Text style={s.count}>{description.length}/500</Text><Label text="가격" /><View style={s.amount}><TextInput value={amount} onChangeText={setAmount} keyboardType="number-pad" style={s.amountInput} placeholder="가격을 입력하세요" placeholderTextColor="#89948F" /><Text style={s.won}>원</Text></View>{error ? <Text style={s.error}>{error}</Text> : null}<Pressable disabled={saving} onPress={save} style={[s.primary, s.formButton, saving && s.disabled]}>{saving ? <ActivityIndicator color="#fff" /> : <Text style={s.primaryText}>{product ? '수정 완료' : '판매글 등록'}</Text>}</Pressable></ScrollView></KeyboardAvoidingView>;
}

function ChatList({ session, onOpen }: { session: AuthSession; onOpen: (thread: ChatThread) => void }) {
  const [threads, setThreads] = useState<ChatThread[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
  async function reload() { setLoading(true); setError(''); try { setThreads(await chatApi.threads(session)); } catch (caught) { setError(caught instanceof Error ? caught.message : '채팅을 불러오지 못했습니다.'); } finally { setLoading(false); } }
  useEffect(() => { reload(); }, []);
  return <View style={s.flex}><View style={s.header}><Brand compact /><Pressable onPress={reload} style={s.refresh}><Text style={s.refreshText}>새로고침</Text></Pressable></View><Text style={s.pageTitle}>거래 채팅</Text><ScrollView contentContainerStyle={s.list}>{loading ? <Loading label="대화를 불러오는 중이에요" /> : error ? <Empty title="채팅을 불러오지 못했어요" body={error} action="다시 시도" onAction={reload} /> : threads.length ? threads.map((thread) => <Pressable key={thread.id} onPress={() => onOpen(thread)} style={s.thread}><Thumbnail product={thread.product} /><View style={s.threadInfo}><Text style={s.threadName}>{thread.otherUser.nickname}</Text><Text numberOfLines={1} style={s.threadProduct}>{thread.product.title}</Text><Text numberOfLines={1} style={s.threadMessage}>{thread.lastMessage}</Text></View><Text style={s.threadDate}>{relative(thread.createdAt)}</Text></Pressable>) : <Empty title="아직 거래 채팅이 없어요" body="상품 상세에서 판매자에게 채팅을 시작해 보세요." />}</ScrollView></View>;
}

function ChatRoom({ session, target, onBack }: { session: AuthSession; target: ChatTarget; onBack: () => void }) {
  const [messages, setMessages] = useState<Awaited<ReturnType<typeof chatApi.messages>>>([]); const [value, setValue] = useState(''); const [loading, setLoading] = useState(true); const [sending, setSending] = useState(false); const [error, setError] = useState('');
  async function reload() { setLoading(true); try { setMessages(await chatApi.messages(target.product.id, target.otherUser.id, session)); } catch (caught) { setError(caught instanceof Error ? caught.message : '대화를 불러오지 못했습니다.'); } finally { setLoading(false); } }
  useEffect(() => { reload(); }, [target.product.id, target.otherUser.id]);
  async function send() { const content = value.trim(); if (!content || sending) return; setSending(true); setError(''); try { const message = await chatApi.send(target.product.id, target.otherUser.id, content, session); setMessages((current) => [...current, message]); setValue(''); } catch (caught) { setError(caught instanceof Error ? caught.message : '메시지를 보내지 못했습니다.'); } finally { setSending(false); } }
  return <KeyboardAvoidingView style={s.flex} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><TopBar title={target.otherUser.nickname} onBack={onBack} /><View style={s.chatProduct}><Text numberOfLines={1} style={s.chatProductTitle}>{target.product.title}</Text><Text style={s.chatProductPrice}>{price(target.product.askingPrice)}</Text></View><ScrollView contentContainerStyle={s.messages}>{loading ? <Loading label="대화를 불러오는 중이에요" /> : messages.length ? messages.map((message) => <View key={message.id} style={[s.bubble, message.senderId === session.user.id ? s.mine : s.other]}><Text style={[s.bubbleText, message.senderId === session.user.id && s.mineText]}>{message.content}</Text><Text style={[s.bubbleTime, message.senderId === session.user.id && s.mineText]}>{time(message.createdAt)}</Text></View>) : <Text style={s.noMessages}>첫 메시지로 거래를 시작해 보세요.</Text>}</ScrollView>{error ? <Text style={s.chatError}>{error}</Text> : null}<View style={s.composer}><TextInput value={value} onChangeText={setValue} style={s.messageInput} placeholder="메시지를 입력하세요" placeholderTextColor="#89948F" multiline maxLength={2000} /><Pressable disabled={sending || !value.trim()} onPress={send} style={[s.send, (sending || !value.trim()) && s.disabled]}><Text style={s.sendText}>전송</Text></Pressable></View></KeyboardAvoidingView>;
}

function Favorites({ products, onBack, onSelect }: { products: Product[]; onBack: () => void; onSelect: (p: Product) => void }) { return <View style={s.flex}><TopBar title="찜한 상품" onBack={onBack} /><ScrollView contentContainerStyle={s.list}>{products.length ? products.map((p) => <ProductRow key={p.id} product={p} favorite onSelect={() => onSelect(p)} onFavorite={() => undefined} />) : <Empty title="찜한 상품이 없어요" body="상품의 하트를 눌러 관심 상품을 모아 보세요." />}</ScrollView></View>; }
function Settings({ loginId, favorites, onSelect, onSignOut }: { loginId: string; favorites: Product[]; onSelect: (p: Product) => void; onSignOut: () => Promise<void> }) { return <ScrollView contentContainerStyle={s.settings}><Brand compact /><Text style={s.settingsTitle}>내 설정</Text><View style={s.profile}><View style={s.avatar}><Text style={s.avatarText}>{loginId.charAt(0).toUpperCase()}</Text></View><Text style={s.sellerName}>{loginId}</Text></View><Text style={s.settingTitle}>찜한 상품 {favorites.length}개</Text>{favorites.length ? favorites.map((p) => <Pressable key={p.id} onPress={() => onSelect(p)} style={s.settingProduct}><Text style={s.productTitle}>{p.title}</Text><Text style={s.price}>{price(p.askingPrice)}</Text></Pressable>) : <Text style={s.hint}>아직 찜한 상품이 없어요.</Text>}<Pressable onPress={onSignOut} style={s.secondary}><Text style={s.secondaryText}>로그아웃</Text></Pressable></ScrollView>; }
function Nav({ active, onSelect }: { active: Tab; onSelect: (tab: Tab) => void }) { const items: { id: Tab; icon: string; label: string }[] = [{ id: 'home', icon: '⌂', label: '홈' }, { id: 'products', icon: '▣', label: '상품' }, { id: 'chat', icon: '◌', label: '채팅' }, { id: 'settings', icon: '⚙', label: '설정' }]; return <View style={s.nav}>{items.map((item) => <Pressable key={item.id} onPress={() => onSelect(item.id)} style={s.navItem}><Text style={[s.navIcon, active === item.id && s.navOn]}>{item.icon}</Text><Text style={[s.navLabel, active === item.id && s.navOn]}>{item.label}</Text></Pressable>)}</View>; }
function TopBar({ title, onBack }: { title: string; onBack: () => void }) { return <View style={s.topBar}><Pressable onPress={onBack} style={s.back}><Text style={s.backText}>‹</Text></Pressable><Text style={s.topTitle}>{title}</Text><View style={s.back} /></View>; }
function Brand({ compact = false }: { compact?: boolean }) { return <View style={s.brandRow}><View style={[s.brandMark, compact && s.brandMarkSmall]}><Text style={s.brandMarkText}>RAM</Text></View><Text style={[s.brand, compact && s.brandSmall]}>RAMarket</Text></View>; }
function Thumbnail({ product, large = false }: { product: Pick<Product, 'id' | 'imagePaths'>; large?: boolean }) { const uri = imageUrl(product.imagePaths[0]); return uri ? <Image source={{ uri }} style={[s.thumbnail, large && s.thumbnailLarge]} /> : <View style={[s.thumbnail, large && s.thumbnailLarge, product.id.charCodeAt(0) % 2 ? s.toneA : s.toneB]}><View style={s.ram}><Text style={s.ramText}>RAM</Text></View></View>; }
function Choice({ label, values, selected, onSelect }: { label: string; values: string[]; selected: string; onSelect: (value: string) => void }) { return <View style={s.choice}><Text style={s.choiceLabel}>{label}</Text><View style={s.choiceRow}>{values.map((value) => <Pressable key={value} onPress={() => onSelect(value)} style={[s.choiceButton, selected === value && s.choiceOn]}><Text style={[s.choiceText, selected === value && s.choiceTextOn]}>{value}</Text></Pressable>)}</View></View>; }
function Label({ text }: { text: string }) { return <Text style={s.label}>{text}</Text>; }
function Loading({ label }: { label: string }) { return <View style={s.loading}><ActivityIndicator color={green} /><Text style={s.emptyBody}>{label}</Text></View>; }
function Empty({ title, body, action, onAction }: { title: string; body: string; action?: string; onAction?: () => void }) { return <View style={s.empty}><Text style={s.emptyTitle}>{title}</Text><Text style={s.emptyBody}>{body}</Text>{action && onAction ? <Pressable onPress={onAction} style={s.emptyButton}><Text style={s.emptyButtonText}>{action}</Text></Pressable> : null}</View>; }
function price(value: number) { return `${value.toLocaleString('ko-KR')}원`; }
function relative(value: string) { const minutes = Math.max(0, Math.floor((Date.now() - new Date(value).getTime()) / 60000)); return minutes < 1 ? '방금 전' : minutes < 60 ? `${minutes}분 전` : minutes < 1440 ? `${Math.floor(minutes / 60)}시간 전` : `${Math.floor(minutes / 1440)}일 전`; }
function time(value: string) { return new Intl.DateTimeFormat('ko-KR', { hour: 'numeric', minute: '2-digit' }).format(new Date(value)); }

const s = StyleSheet.create({
  safe:{flex:1,backgroundColor:'#fff'},flex:{flex:1},login:{flexGrow:1,justifyContent:'center',padding:24},loginTitle:{marginTop:44,color:'#16201F',fontSize:30,fontWeight:'800'},loginSub:{marginTop:8,marginBottom:36,color:'#68736F',fontSize:15},brandRow:{flexDirection:'row',alignItems:'center',gap:9},brandMark:{width:70,height:40,alignItems:'center',justifyContent:'center',borderRadius:7,backgroundColor:green},brandMarkSmall:{width:43,height:25},brandMarkText:{color:'#fff',fontWeight:'900',fontSize:12},brand:{color:green,fontSize:30,fontWeight:'800'},brandSmall:{fontSize:21},label:{marginTop:18,marginBottom:8,color:green,fontSize:15,fontWeight:'800'},input:{height:54,paddingHorizontal:15,borderWidth:1,borderColor:'#D7DDDA',borderRadius:14,color:'#16201F',fontSize:16},primary:{height:57,alignItems:'center',justifyContent:'center',borderRadius:15,backgroundColor:green},primaryText:{color:'#fff',fontSize:17,fontWeight:'800'},secondary:{minHeight:53,alignItems:'center',justifyContent:'center',marginTop:14,borderWidth:1,borderColor:green,borderRadius:14},secondaryText:{color:green,fontSize:16,fontWeight:'800'},error:{marginTop:12,padding:11,borderRadius:10,color:'#B9382F',backgroundColor:'#FFF0F0',fontSize:13},disabled:{opacity:.45},header:{height:62,flexDirection:'row',alignItems:'center',justifyContent:'space-between',paddingHorizontal:20},favoriteShortcut:{minWidth:45,height:37,flexDirection:'row',alignItems:'center',justifyContent:'center',gap:3,paddingHorizontal:9,borderRadius:12,backgroundColor:'#FFF1F2'},favoriteCount:{color:'#B93848',fontWeight:'800'},pageTitle:{marginTop:14,paddingHorizontal:20,color:'#16201F',fontSize:27,fontWeight:'800'},searchBox:{height:52,flexDirection:'row',alignItems:'center',margin:20,marginBottom:0,paddingHorizontal:14,borderRadius:15,backgroundColor:'#F1F6F4'},searchIcon:{marginRight:8,color:'#68736F',fontSize:28},searchInput:{flex:1,color:'#16201F',fontSize:15},categories:{gap:8,paddingHorizontal:20,paddingVertical:11},category:{paddingHorizontal:16,paddingVertical:9,borderWidth:1,borderColor:'#D7DDDA',borderRadius:20},categoryOn:{borderColor:green,backgroundColor:green},categoryText:{color:'#68736F',fontWeight:'700'},categoryTextOn:{color:'#fff'},list:{flexGrow:1,paddingHorizontal:20,paddingBottom:100},productRow:{minHeight:126,flexDirection:'row',alignItems:'center',borderBottomWidth:1,borderBottomColor:'#EDF0EF'},thumbnail:{width:96,height:96,alignItems:'center',justifyContent:'center',marginRight:13,borderRadius:15,overflow:'hidden'},thumbnailLarge:{width:'100%',height:280,marginRight:0,borderRadius:0},toneA:{backgroundColor:'#E4F0E9'},toneB:{backgroundColor:'#DCE2E0'},ram:{width:'78%',height:'37%',alignItems:'center',justifyContent:'center',borderRadius:5,backgroundColor:'#1B2724',transform:[{rotate:'-10deg'}]},ramText:{color:'#DDF3ED',fontSize:11,fontWeight:'900'},productInfo:{flex:1,paddingRight:24},memory:{marginBottom:5,color:green,fontSize:12,fontWeight:'800'},productTitle:{color:'#16201F',fontSize:16,fontWeight:'700',lineHeight:21},price:{marginTop:5,color:green,fontSize:17,fontWeight:'800'},meta:{marginTop:6,color:'#89948F',fontSize:12},heartButton:{position:'absolute',right:0,bottom:20,padding:5},heart:{color:'#89948F',fontSize:27},heartOn:{color:'#CF4A58'},sell:{position:'absolute',right:20,bottom:15,paddingHorizontal:19,paddingVertical:14,borderRadius:26,backgroundColor:green,elevation:5},sellText:{color:'#fff',fontSize:16,fontWeight:'800'},topBar:{height:58,flexDirection:'row',alignItems:'center',justifyContent:'space-between',paddingHorizontal:13},back:{width:40,height:40,alignItems:'center',justifyContent:'center'},backText:{marginTop:-5,color:'#16201F',fontSize:38,fontWeight:'300'},topTitle:{color:'#16201F',fontSize:17,fontWeight:'800'},detail:{padding:20},detailTitle:{color:'#16201F',fontSize:24,fontWeight:'800',lineHeight:31},detailPrice:{marginTop:18,color:'#16201F',fontSize:25,fontWeight:'800'},divider:{height:1,marginVertical:21,backgroundColor:'#E9EEEC'},seller:{flexDirection:'row',alignItems:'center',gap:11},avatar:{width:45,height:45,alignItems:'center',justifyContent:'center',borderRadius:23,backgroundColor:'#DDF3ED'},avatarText:{color:green,fontSize:18,fontWeight:'800'},sellerName:{color:'#16201F',fontSize:16,fontWeight:'800'},description:{marginTop:21,color:'#40514C',fontSize:15,lineHeight:24},actions:{flexDirection:'row',alignItems:'center',padding:17,borderTopWidth:1,borderTopColor:'#E9EEEC'},actionHeart:{width:47,alignItems:'center',borderRightWidth:1,borderRightColor:'#E9EEEC'},actionInfo:{flex:1,paddingLeft:13},actionPrice:{color:'#16201F',fontSize:16,fontWeight:'800'},actionMeta:{marginTop:2,color:'#89948F',fontSize:11},actionButton:{minWidth:105,alignItems:'center',justifyContent:'center',paddingHorizontal:14,paddingVertical:14,borderRadius:12,backgroundColor:green},actionButtonText:{color:'#fff',fontSize:15,fontWeight:'800'},form:{padding:20,paddingBottom:45},imagePicker:{height:128,alignItems:'center',justifyContent:'center',borderWidth:1,borderStyle:'dashed',borderColor:'#A8BAB4',borderRadius:16,backgroundColor:'#F8FBFA'},imageIcon:{color:green,fontSize:32},imageText:{marginTop:5,color:'#40514C',fontSize:15,fontWeight:'800'},hint:{marginBottom:12,color:'#68736F',fontSize:13},photos:{gap:10,paddingTop:12},photoWrap:{position:'relative'},photo:{width:75,height:75,borderRadius:10,backgroundColor:'#EAF4F1'},remove:{position:'absolute',top:-6,right:-6,width:22,height:22,alignItems:'center',justifyContent:'center',borderRadius:11,backgroundColor:'#16201F'},removeText:{color:'#fff',fontSize:17},formInput:{minHeight:53,paddingHorizontal:14,paddingVertical:12,borderWidth:1,borderColor:'#D7DDDA',borderRadius:13,color:'#16201F',fontSize:15},descriptionInput:{height:125},count:{marginTop:5,color:'#89948F',fontSize:12,textAlign:'right'},choice:{marginBottom:14},choiceLabel:{marginBottom:8,color:'#40514C',fontSize:14,fontWeight:'800'},choiceRow:{flexDirection:'row',flexWrap:'wrap',gap:8},choiceButton:{minWidth:64,alignItems:'center',paddingHorizontal:13,paddingVertical:10,borderWidth:1,borderColor:'#D7DDDA',borderRadius:12},choiceOn:{borderColor:green,backgroundColor:'#EAF4F1'},choiceText:{color:'#68736F',fontWeight:'700'},choiceTextOn:{color:green},amount:{height:53,flexDirection:'row',alignItems:'center',paddingRight:14,borderWidth:1,borderColor:'#D7DDDA',borderRadius:13},amountInput:{flex:1,height:'100%',paddingHorizontal:14,color:'#16201F',fontSize:15},won:{color:'#40514C',fontWeight:'700'},formButton:{marginTop:26},refresh:{padding:9},refreshText:{color:green,fontSize:13,fontWeight:'800'},thread:{minHeight:101,flexDirection:'row',alignItems:'center',borderBottomWidth:1,borderBottomColor:'#EDF0EF'},threadInfo:{flex:1,paddingRight:12},threadName:{color:'#16201F',fontSize:16,fontWeight:'800'},threadProduct:{marginTop:3,color:green,fontSize:12,fontWeight:'700'},threadMessage:{marginTop:5,color:'#68736F',fontSize:13},threadDate:{alignSelf:'flex-start',marginTop:22,color:'#89948F',fontSize:11},chatProduct:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',marginHorizontal:16,padding:12,borderRadius:11,backgroundColor:'#F1F6F4'},chatProductTitle:{flex:1,paddingRight:8,color:'#40514C',fontSize:13,fontWeight:'700'},chatProductPrice:{color:green,fontSize:13,fontWeight:'800'},messages:{flexGrow:1,padding:16,gap:9},bubble:{maxWidth:'78%',paddingHorizontal:13,paddingVertical:10,borderRadius:15},mine:{alignSelf:'flex-end',backgroundColor:green,borderBottomRightRadius:3},other:{alignSelf:'flex-start',backgroundColor:'#EEF2F0',borderBottomLeftRadius:3},bubbleText:{color:'#24312D',fontSize:15,lineHeight:20},mineText:{color:'#fff'},bubbleTime:{marginTop:4,color:'#68736F',fontSize:10,textAlign:'right'},noMessages:{marginTop:30,color:'#89948F',textAlign:'center'},composer:{minHeight:68,flexDirection:'row',alignItems:'flex-end',gap:8,padding:10,borderTopWidth:1,borderTopColor:'#E9EEEC'},messageInput:{flex:1,maxHeight:100,paddingHorizontal:13,paddingVertical:10,borderRadius:16,backgroundColor:'#F1F6F4',color:'#16201F',fontSize:15},send:{paddingHorizontal:13,paddingVertical:12,borderRadius:12,backgroundColor:green},sendText:{color:'#fff',fontWeight:'800'},chatError:{marginHorizontal:12,marginBottom:4,color:'#B9382F',fontSize:12},nav:{height:69,flexDirection:'row',paddingTop:8,borderTopWidth:1,borderTopColor:'#E9EEEC'},navItem:{flex:1,alignItems:'center'},navIcon:{color:'#89948F',fontSize:25},navLabel:{color:'#89948F',fontSize:12,fontWeight:'700'},navOn:{color:green},loading:{flex:1,alignItems:'center',justifyContent:'center',gap:10,paddingTop:50},empty:{flex:1,alignItems:'center',justifyContent:'center',paddingHorizontal:34},emptyTitle:{color:'#16201F',fontSize:17,fontWeight:'800',textAlign:'center'},emptyBody:{marginTop:7,color:'#68736F',fontSize:14,lineHeight:20,textAlign:'center'},emptyButton:{marginTop:17,paddingHorizontal:15,paddingVertical:10,borderRadius:10,backgroundColor:'#EAF4F1'},emptyButtonText:{color:green,fontWeight:'800'},settings:{flexGrow:1,padding:24},settingsTitle:{marginTop:38,color:'#16201F',fontSize:27,fontWeight:'800'},profile:{flexDirection:'row',alignItems:'center',gap:12,marginTop:19,marginBottom:32,padding:17,borderRadius:17,backgroundColor:'#EAF4F1'},settingTitle:{marginBottom:9,color:'#40514C',fontSize:15,fontWeight:'800'},settingProduct:{paddingVertical:13,borderBottomWidth:1,borderBottomColor:'#EDF0EF'}
});
